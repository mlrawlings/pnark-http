{
  "name": "pnark-http",
  "version": "1.0.0",
  "description": "A plugin for pnark that shows requests made on the server with node's `http` module in the browser with a timeline chart and additional information about the requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mlrawlings/pnark-http.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "instrumitter": "^1.1.0"
  }
}
